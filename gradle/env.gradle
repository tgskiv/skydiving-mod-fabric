ext.env = new Properties()
def envFile = file(".env")
def exampleEnvFile = file("example.env")

if (envFile.exists()) {
    println "📦 Loading .env"
    envFile.withInputStream { env.load(it) }
} else if (exampleEnvFile.exists()) {
    println "📦 Loading example.env (fallback)"
    exampleEnvFile.withInputStream { env.load(it) }
} else {
    println "⚠ No .env or example.env found. Some tasks may not work."
}
